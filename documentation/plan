1. Реализовать обработку ролей 0 и 2.
Для роли 2 - добавить рассчет( если он есть) и добавление в таблицу информации о зарпалате. 
Для роли 0 - если есть набор данных - указать их в общей таблице и затем зарплата(фиксированная). 
Если набора данных нет( или представлены одной записью за месяц), то добавляется запись в таблицу с зарплатой.

2. Реализовать рандомизатор минут и  секунд при вводе времени метки. 
Если введен час, но не введены минуты и секнуды - минуты и секунды добавляются.
Если не введено ничего - то рассчитывается временная метка, таким образом, что бы рабочее время составило от 8 до 9 
часов( учитывая случайную минуту и секунуду)

3. Реализовать доступ к произвольной записи. 
Вводится число месяца, после чего система выводит фамилии работников за этот день. После чего система ждет
ввода id работника, который работает в выбранный день. После чего система выводит информацию о метке прихода и ухода. 
Система предлогает ввести новое время метки.
Этот функционал работает после анализа и исправления исходных данных и до построения таблицы в excel и html-файлов. 

4. Реализовать возможность создания новой записи в любом дне месяца, даже если там нет записей ни для одного работника.
Этот функционал работает после анализа и исправления исходных данных и до построения таблицы в excel и html-файлов.

5. Добавить возможность сохранения данных за каждый месяц в SQL базу данных.
